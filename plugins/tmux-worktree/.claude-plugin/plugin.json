{
  "name": "tmux-worktree",
  "version": "3.1.0",
  "description": "Git worktree workflow with tmux integration for parallel Claude Code sessions",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bun run ${CLAUDE_PLUGIN_ROOT}/src/ensure-gtrconfig.ts"
          }
        ]
      }
    ]
  },
  "mcpServers": {
    "worktree": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/src/mcp/server.ts"]
    }
  }
}
